export enum UserRole {
  STUDENT = 'Student',
  ADMIN = 'Admin',
  MODERATOR = 'Moderator',
}

export enum UserStatus {
  ACTIVE = 'Active',
  SUSPENDED = 'Suspended',
  BANNED = 'Banned',
  INACTIVE = 'Inactive',
}

export enum SubscriptionType {
  FREE = 'Free',
  MONTHLY = 'Monthly',
  YEARLY = 'Yearly',
}
